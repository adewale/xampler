# Pages 23 — Functions

Pages is primarily a frontend/static hosting primitive. This example shows file-based Pages Functions in TypeScript plus a Pythonic mental model document. Run with `npx wrangler pages dev public`.
