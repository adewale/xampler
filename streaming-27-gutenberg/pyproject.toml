[project]
name = "xampler-streaming-27-gutenberg"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "cfboundary @ git+https://github.com/adewale/cfboundary@v0.1.11",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
